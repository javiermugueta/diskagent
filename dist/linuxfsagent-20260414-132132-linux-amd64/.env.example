# Requeridas para salida OCI (output=oci_metrics o both)
ORACLE_METRICS_NAMESPACE=custom_diskagent
ORACLE_COMPARTMENT_OCID=ocid1.compartment.oc1..example

# Opcionales
# ORACLE_RESOURCE_GROUP=disk-agent
# ORACLE_AUTH_MODE=config

# Ejemplo de ejecución
# ./run-linuxfsagent.sh --once --output stdout
# ./run-linuxfsagent.sh --interval 60s --output both
